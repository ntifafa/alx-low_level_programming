#include <stdio.h>
#include <string.h>
/**
 * main - Print out lines with put function
 * Return: 0 (success)
 */
int main(void)
{
char str[] = "\"Programming is like building a multilingual puzzle";
puts(str);
return (0);
}
